# Conservative project defaults. This layer is loaded only for a trusted repo.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_doc_max_bytes = 32768
web_search = "cached"

[sandbox_workspace_write]
network_access = false

[features]
goals = true
shell_snapshot = true
